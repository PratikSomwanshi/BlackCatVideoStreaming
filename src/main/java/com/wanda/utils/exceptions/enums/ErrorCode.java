package com.wanda.utils.exceptions.enums;

public enum ErrorCode {
    GENERAL_ERROR,
    FILE_NOT_FOUND,
    ALGORITHM_ERROR,
    TOKEN_NOT_FOUND,
    TOKEN_INVALID,
    TOKEN_EXPIRED,
    INVALID_GOOGLE_OAUTH_TOKEN,
    USER_NOT_FOUND,
    BAD_CREDENTIALS,
    USER_NOT_PREMIUM
}
